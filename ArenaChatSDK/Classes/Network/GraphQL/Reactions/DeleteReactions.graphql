mutation DeleteReactions(
    $itemId:ID!,
    $reaction:String!,
    $userId:ID!
) {
    deleteReaction(input: {
        itemId: $itemId,
        reaction: $reaction,
        userId: $userId,
    })
}
